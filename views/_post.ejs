<li>
    <%if(locals.user && locals.user.id==i.user.id){%>
        <small><a href="/posts/destroy/<%=i.id%>">Delete Post</a></small>
    <%}%>
        <%=i.content%>
            <%=i.user.name%>
                <div class="post-comment">
                    <form action="/comments/add_comment" method="POST">
                        <input type="text" name="content" placeholder="Add comment here.." required>
                        <input type="hidden" name="post" value="<%=i._id%>">
                        <button type="submit">Add comment</button>
                    </form>
                    <div id="post-comments-list">
                        <ul>
                            <%for(comment of i.comments){%>
                                <%- include('_comment.ejs');%>
                            <%}%>
                            </ul>
                        </div>
                </div>
</li>